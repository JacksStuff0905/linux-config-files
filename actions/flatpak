# Perform flatpak operations (sudo flatpack will be inserted before the command)

# Install flathub
remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

# Install OpenRGB
install flathub org.openrgb.OpenRGB
sudo ln -s /var/lib/flatpak/exports/bin/org.openrgb.OpenRGB /usr/bin/openrgb
